-- Write code below 💖

-- SELECT * FROM shows; 
-- This would select all collumns from the table name shows.

SELECT name, genre FROM shows;
-- This would select the collumns name and genre from the table name shows.
